version: "2"

services:
  db:
    hostname: "db"
    image: "mongo"
    volumes:
      - "../../volumes/mongodb:/data/db"
    ports:
      - "27017:27017"

  app:
    depends_on:
      - "db"
    image: "app"
    build:
      context: "./services/app"
      dockerfile: "docker/Dockerfile-develop"
    ports:
      - "3000:3000"
      - "3123:3123"
      - "8080:8080"
    volumes:
      - "./services/app:/src/"
